# Core
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Claude API (judgement analysis only - not used for scraping/parsing)
# PINNED 1 Sep 2026 to the version running in the VPS container. Dockerfile
# re-installs on every rebuild, so an unpinned '>=' pulls whatever is newest
# that day. The 27 Jul 2026 ThinkingBlock incident was exactly a client/model
# behaviour change silently killing the judgement layer. Bump deliberately.
anthropic==0.122.0

# Telegram
python-telegram-bot>=20.7

# Scheduling
schedule>=1.2.0
# schedule .at(time, tz) requires pytz for timezone-aware firing.
# Without it the job fires on the container's UTC clock, so a UK
# clock time drifts by the BST/GMT offset (19:00 vs 18:00 UTC).
pytz>=2024.1

# Logging
python-json-logger>=2.0.0
